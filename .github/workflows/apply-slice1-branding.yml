# Neutralized: branding remainder paused; this workflow was fail-mailing on push.
name: Apply Slice 1 branding remainder
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
