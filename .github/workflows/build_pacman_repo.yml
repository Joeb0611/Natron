# Neutralized: inherited MinGW pacman-repo workflow cannot succeed on this account.
name: Build pacman repo
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
