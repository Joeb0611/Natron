# One-shot canceler used 2026-09-16. Kept dispatch-only so it cannot fail-mail.
name: Cancel inherited runs
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
