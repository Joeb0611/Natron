# Neutralized: inherited from NatronGitHub/Natron.
# Cannot succeed on this account (public pacman overlay + 2.5.0 golden-log pass criterion).
name: Build Installer
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
