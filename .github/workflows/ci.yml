# Neutralized: inherited Natron "Tests" workflow (Travis-era leftover on GitHub Actions).
# Default branch is RB-2.6; do not auto-run this on the fork.
name: Tests
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
