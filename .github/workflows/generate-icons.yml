# Neutralized: first run failed because tools/generate-natronplus-icons.py was not in the tree yet.
name: Generate Natron+ icons
on:
  workflow_dispatch:
jobs:
  disabled:
    name: Neutralized
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo neutralized
